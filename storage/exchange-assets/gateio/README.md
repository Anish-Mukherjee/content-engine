# Gate.io press-kit assets

Drop Gate.io-licensed images here (`.jpg`, `.png`, `.webp`). See
`../README.md` for the full sourcing workflow and brand-licensing notes.

Brand assets are typically published at:
https://www.gate.io/ — search "press", "media", or "brand".

Slug note: matched against query strings as `gate.io`, `gate io`, or
`gateio` (see `QUERY_ALIASES` in `src/integrations/local-press-kit/index.ts`).
